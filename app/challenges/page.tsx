import Link from 'next/link';
import { Filter, Map, Search } from 'lucide-react';
import { challenges } from '@/lib/mockData';
import { ChallengeCard, SiteNav, Reveal } from '@/components/ui';

export default function Challenges(){ return <><SiteNav/><main className="section"><div className="container"><Reveal><div className="eyebrow">Challenge network</div><div style={{display:'flex',justifyContent:'space-between',alignItems:'end',gap:20,margin:'10px 0 24px'}}><div><h1 style={{fontSize:58,letterSpacing:'-.06em',lineHeight:.95,margin:0}}>The problem map.</h1><p className="muted" style={{maxWidth:670,lineHeight:1.7}}>Every report becomes structured, discoverable and actionable. Use filters to surface the highest-impact challenges and open the full AI analysis.</p></div><Link className="btn btn-primary" href="/report">+ New challenge</Link></div></Reveal><div className="card panel" style={{marginBottom:18}}><div style={{display:'flex',flexWrap:'wrap',gap:10}}><div className="soft-card" style={{padding:'11px 14px',display:'flex',alignItems:'center',gap:8,flex:1,minWidth:230}}><Search size={16}/><span className="muted">Search challenges...</span></div><div className="btn btn-ghost" style={{borderRadius:14}}><Filter size={16}/> Priority</div><div className="btn btn-ghost" style={{borderRadius:14}}><Map size={16}/> District</div><div className="btn btn-ghost" style={{borderRadius:14}}>Domain</div></div></div><div className="challenge-grid">{challenges.map(c=><ChallengeCard c={c} key={c.id}/>)}</div></div></main></> }
